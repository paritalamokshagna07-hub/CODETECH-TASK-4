# -------- Task 4: ML Spam Detection --------
import pandas as pd
from sklearn.feature_extraction.text import CountVectorizer
from sklearn.model_selection import train_test_split
from sklearn.naive_bayes import MultinomialNB
from sklearn.metrics import accuracy_score, classification_report

# Load dataset (download 'spam.csv' with columns ['label','message'])
df = pd.read_csv("spam.csv", encoding='latin-1')[['v1','v2']]
df.columns = ['label', 'message']

X = df['message']
y = df['label'].map({'ham':0, 'spam':1})

# Convert text to features
cv = CountVectorizer()
X_vec = cv.fit_transform(X)

# Train-test split
X_train, X_test, y_train, y_test = train_test_split(X_vec, y, test_size=0.2, random_state=42)

# Train model
model = MultinomialNB()
model.fit(X_train, y_train)

# Evaluate
y_pred = model.predict(X_test)
print("✅ Accuracy:", accuracy_score(y_test, y_pred))
print(classification_report(y_test, y_pred))

# Test with custom input
msg = ["Congratulations! You won a free ticket 🎟️"]
msg_vec = cv.transform(msg)
print("Prediction:", "Spam" if model.predict(msg_vec)[0]==1 else "Ham")
